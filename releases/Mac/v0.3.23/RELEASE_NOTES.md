# 智额 0.3.23 (build 26)

- **日期**：2026-08-09  
- **标签**：`v0.3.23`  

## 本版

- **静默更新（对齐智余）**  
  设置 → **检查更新** → 下 `.pkg` → `pkgutil` 解包 → App 退出后 `ditto` 覆盖 → 自动打开。  
  - 无二次确认弹窗  
  - **不请求管理员密码**（不可写则设置页报错）  
  - 日志：`~/Library/Logs/SmartQuota/update.log`  

## 安装

| GitHub 资产 | 用法 |
|-------------|------|
| **SmartQuota-0.3.23.dmg** | 拖到 Applications |
| **SmartQuota-0.3.23.pkg** | 安装向导；应用内更新优先此包 |

已装旧版：设置 → **检查更新**。

macOS 15.0+。临时签名（ad-hoc）。
